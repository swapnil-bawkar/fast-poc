import { MermaidEditorComponent } from './mermaid-editor/mermaid-editor.component';
import { LaunchApplicationComponent } from './launch-application/launch-application.component';

export const CONTAINERS = [MermaidEditorComponent, LaunchApplicationComponent];
